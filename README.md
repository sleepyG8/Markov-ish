# Markov-ish
My own take a procedural text generation using the concept of a markov chain. When ran, it will print a random story to the console. Each story is randomly generated.

What I did:

==============================

Uses hardware randomization to randomize hash map buckets each run giving a unique output each run


